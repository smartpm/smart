
- PreRequires handling
- Ordering system
- Download restoring (timestamp or digest) and
  uncompressing in fetcher
- Dynamic auto-fetching of local data even when caching=NEVER.
- Transaction reporting system, which computes differences between
  transactions, and changes being introduced by a transaction
  (upgrades, downgrades, reinstalls, etc).
- Implement package markup/flagging.
